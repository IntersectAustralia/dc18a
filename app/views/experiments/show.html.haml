%h2 Experiment View

.container
  = render_field "Experiment Database ID", @experiment.id
  = render_field "Experiment ID", @experiment.expt_id
  = render_field "Experiment Name", @experiment.expt_name
  = render_field "Created Date", @experiment.created_date
  = render_field "Instrument Name", @experiment.instrument
  = render_field "Belongs to Project", @experiment.project.name
  = render_field "Owner", @experiment.user.full_name
  = render_field "Lab Book No.", @experiment.lab_book_no
  = render_field "Page No.", @experiment.page_no
  = render_field "Cell Type Or Tissue", @experiment.cell_type_or_tissue
  = render_field "Experiment Type", @experiment.expt_type
  = render_field "Slides", @experiment.slides
  = render_field "Dishes", @experiment.dishes
  = render_field "Multiwell Chambers", @experiment.multiwell_chambers
  = render_field "Other", @experiment.other
  = render_field "Other (Specify)", @experiment.other_text
  = render_field "Fluorescent proteins", @experiment.has_fluorescent_proteins
  = render_field "Fluorescent Proteins (Specify)", @experiment.fluorescent_proteins.pluck(:name)
  = render_field "Specific dyes", @experiment.has_specific_dyes
  = render_field "Specific Dyes (Specify)", @experiment.specific_dyes.pluck(:name)
  = render_field "Immunofluorescence", @experiment.has_immunofluorescence
  = render_field "Specific Dyes (Specify)", @experiment.immunofluorescences.pluck(:name)


= link_to 'View Project Details', project_path(@experiment.project), :id => "back_to_project_page"


