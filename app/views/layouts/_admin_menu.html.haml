- if user_signed_in? && can?(:admin, User)
  %ul.nav.pull-right
    %li.dropdown
      = link_to administration_dropdown_menu, '#', :class => "dropdown-toggle", 'data-toggle' => "dropdown"
      %ul.dropdown-menu
        %li= link_to "User List", users_path
        %li= link_to "Access Requests", access_requests_users_path
        %li= link_to "Reporting", reports_users_path

